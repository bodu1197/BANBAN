# 푸시 전 풀 품질 게이트 (#35 완료로 활성화):
# ESLint(--max-warnings 0) + TypeScript + GC Scanner(CLAUDE.md 룰) + JSCPD(중복 ≤6%) + Madge(순환 0).
# 경고 74→0 정리 + gc-scan 위반 41→0 수정 완료 후 교체됨. SonarQube 는 SONAR_TOKEN 있을 때만(없으면 skip).
npm run quality-gate
