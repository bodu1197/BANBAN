import PortfolioWriteClient from "@/app/(main)/mypage/artist/portfolios/write/components/PortfolioWriteClient";

export async function renderArtistPortfolioWritePage(): Promise<React.ReactElement> {
    return <PortfolioWriteClient />;
}
