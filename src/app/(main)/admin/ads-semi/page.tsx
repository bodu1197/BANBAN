import AdminAdDashboard from "@/app/(main)/admin/ads/page";

export default function AdminSemiAdDashboard(): React.ReactElement {
    return <AdminAdDashboard artistType="SEMI_PERMANENT" dashboardTitle="반영구 광고 관리 대시보드" />;
}
